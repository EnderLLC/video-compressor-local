import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.localmediatools.app',
  appName: 'Video Compressor',
  webDir: 'out'
};

export default config;
